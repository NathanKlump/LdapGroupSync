processResources {
    filesMatching('**/application.properties') {
        def props = new Properties()
        file('/opt/ou/ldapgroupsync/config/application.properties').withInputStream {
            props.load(it)
        }
        filter(org.apache.tools.ant.filters.ReplaceTokens, tokens: props)
    }
}
